<!-- footer include: Version 1.2.5-1 -->
	<div id="footer"> 
		<em><span id="user_connection">Connected from <TMPL_VAR ESCAPE=HTML NAME=user_ip> and <TMPL_IF NAME=logged_in_user>logged in as <TMPL_VAR ESCAPE=HTML NAME=logged_in_user><TMPL_ELSE>not logged in</TMPL_IF>.</span>
	</div>
	<!-- end of footer include -->
